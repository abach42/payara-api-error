services:
  payara-test:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"  # HTTP Web-Port für deine REST-Endpunkte
      - "4848:4848"  # Payara Admin Console
    volumes:
      - payara_logs:/opt/payara/server/glassfish/domains/domain1/logs
    environment:
      PAYARA_LOG_TO_CONSOLE: "true"

volumes:
  payara_logs: